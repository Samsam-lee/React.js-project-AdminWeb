import React from 'react'
import {FlexDiv} from '../styledFile'

const Loading = () => {
    return (
        <FlexDiv fontSize='20px'>
            Loading...
        </FlexDiv>
    )
}

export default Loading
