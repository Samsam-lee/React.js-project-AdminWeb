import React from 'react'
import {FlexDiv} from '../styledFile'

const Loading = () => {
    return (
        <FlexDiv fontSize='30px'>
            Loading...
        </FlexDiv>
    )
}

export default Loading
